# 面经派（InterviewPai）项目需求说明书

## 一、项目概述

### 1.1 项目名称

面经派（InterviewPai）

### 1.2 项目背景

在技术面试过程中，很多求职者会录制面试音频用于复盘，但这些录音通常较为零散，缺乏结构化整理，难以高效复习和总结。

本项目旨在构建一个 **基于 AI 的面试经验管理平台**，能够将面试录音自动转换为文字，并通过 AI 技术识别其中的 **问题与答案**，形成结构化的面试知识库，帮助用户进行复习与总结。

------

## 二、项目目标

系统需要实现以下目标：

1. 支持用户上传 **面试录音**
2. 自动完成 **语音识别（Audio → Text）**
3. 自动提取 **面试问题与答案**
4. 结构化存储面试内容
5. 提供 **面试经验管理与检索**
6. 支持 **面试复习模式**
7. 所有文件存储到 **MinIO**

------

# 三、技术架构

## 3.1 前端技术栈

- Vue3
- TypeScript
- Vite
- Pinia
- Element Plus
- Axios

主要职责：

- 用户界面展示
- 文件上传
- 面经管理
- 数据展示

------

## 3.2 后端技术栈

- Spring Boot 3
- Spring MVC
- MyBatis Plus
- Spring Security + JWT
- Redis
- MySQL
- MinIO

主要职责：

- API接口
- 业务逻辑
- AI任务调度
- 数据管理

------

## 3.3 数据存储

### MySQL

存储：

- 用户信息
- 面经记录
- 问题答案
- 标签信息

------

### Redis

用于：

1. 登录Token缓存
2. 任务处理状态
3. 热门问题缓存

------

### MinIO

用于存储：

- 音频文件
- 转写文本
- AI总结结果

------

# 四、系统架构

整体架构如下：

```
前端 Vue3
    │
    │ HTTP API
    ▼
SpringBoot 后端
    │
    ├── MySQL
    ├── Redis
    ├── MinIO
    │
    ▼
AI处理模块
    ├── 语音识别
    ├── 问题识别
    └── AI总结
```

------

# 五、核心功能模块

## 5.1 用户模块

### 功能描述

提供用户账号管理。

### 功能列表

1. 用户注册
2. 用户登录
3. 用户信息修改
4. 退出登录

### 数据表

user

字段：

| 字段        | 说明     |
| ----------- | -------- |
| id          | 用户ID   |
| username    | 用户名   |
| password    | 密码     |
| nickname    | 昵称     |
| avatar      | 头像     |
| create_time | 创建时间 |

------

# 5.2 音频上传模块

### 功能描述

用户上传面试录音。

支持格式：

- mp3
- wav
- m4a

上传后：

- 文件保存至 **MinIO**
- 创建音频记录
- 状态标记为 **待处理**

------

### 数据表

audio_record

| 字段        | 说明     |
| ----------- | -------- |
| id          | 音频ID   |
| user_id     | 用户ID   |
| file_url    | 文件地址 |
| duration    | 音频时长 |
| status      | 处理状态 |
| create_time | 创建时间 |

status：

```
UPLOADED
PROCESSING
FINISHED
FAILED
```

------

# 5.3 音频转文字模块（ASR）

### 功能描述

将音频自动转换为文本。

流程：

```
音频上传
   ↓
创建任务
   ↓
语音识别
   ↓
生成文本
```

识别完成后：

- 文本存储到 MinIO
- 数据库存储文本记录

------

### 数据表

audio_transcript

| 字段            | 说明     |
| --------------- | -------- |
| id              | ID       |
| audio_id        | 音频ID   |
| transcript_text | 转写文本 |
| create_time     | 创建时间 |

------

# 5.4 问答识别模块（核心AI）

### 功能描述

从识别出的文本中自动抽取：

- 面试问题
- 面试答案

示例：

原始文本：

```
面试官：什么是线程池？

候选人：线程池是一种线程复用机制...
```

解析后：

```
Q：什么是线程池？
A：线程池是一种线程复用机制...
```

------

### 数据表

qa_record

| 字段       | 说明   |
| ---------- | ------ |
| id         | ID     |
| audio_id   | 音频ID |
| question   | 问题   |
| answer     | 答案   |
| tags       | 标签   |
| confidence | 置信度 |

------

# 5.5 AI总结模块

### 功能描述

系统自动对面试内容进行总结。

生成内容：

- 面试问题列表
- 高频知识点
- 面试总结

示例：

```
本次面试主要涉及：

Java并发
JVM
Spring原理
Redis缓存
```

------

### 数据表

interview_summary

| 字段     | 说明   |
| -------- | ------ |
| id       | ID     |
| audio_id | 音频ID |
| summary  | 总结   |
| keywords | 关键词 |

------

# 5.6 面经管理模块

### 功能描述

用户可以管理自己的面试记录。

支持：

1. 查看面经列表
2. 查看面经详情
3. 删除面经
4. 编辑问题
5. 编辑答案

------

# 5.7 搜索功能

用户可以搜索面试问题。

支持：

- 关键词搜索
- 标签搜索

示例：

```
线程池
Spring
JVM
```

------

# 5.8 标签系统

问题支持添加标签。

示例：

```
Java
并发
JVM
Redis
MySQL
```

------

### 数据表

tag

| 字段 | 说明     |
| ---- | -------- |
| id   | 标签ID   |
| name | 标签名称 |

qa_tag

| 字段   | 说明   |
| ------ | ------ |
| qa_id  | 问题ID |
| tag_id | 标签ID |

------

# 5.9 面试复习模式

提供刷题模式。

展示：

```
问题：什么是AOP？

点击显示答案
```

功能：

- 随机问题
- 按标签复习

------

# 六、文件存储设计

MinIO Bucket：

```
interview
```

目录结构：

```
audio/
    2026/
        xxx.mp3

transcript/
    xxx.txt

summary/
    xxx.json
```

------

# 七、Redis设计

Redis主要用于：

### Token缓存

```
user:token:{userId}
```

------

### 音频处理状态

```
audio:process:{audioId}
```

------

### 热门问题

```
hot:qa:list
```

------

# 八、AI处理流程

系统处理流程：

```
上传音频
   │
   ▼
保存MinIO
   │
   ▼
创建处理任务
   │
   ▼
语音识别
   │
   ▼
文本清洗
   │
   ▼
AI问题抽取
   │
   ▼
结构化存储
   │
   ▼
AI总结
```

------

# 九、API接口设计

## 上传音频

```
POST /api/audio/upload
```

返回：

```
audioId
fileUrl
```

------

## 查询面经列表

```
GET /api/interview/list
```

------

## 查询问题列表

```
GET /api/qa/list
```

参数：

```
audioId
```

------

# 十、前端页面设计

系统主要页面：

### 登录页

功能：

- 登录
- 注册

------

### 首页

展示：

- 面经列表
- 最近面试记录

------

### 上传页面

功能：

- 上传音频
- 查看上传进度

------

### 面经详情页

展示：

- 面试文本
- 问题列表
- AI总结

------

### 复习页面

功能：

- 问题卡片
- 点击查看答案

------

# 十一、非功能需求

### 性能

- 音频上传 ≤ 100MB
- API响应 ≤ 1秒

------

### 安全

- JWT认证
- 用户数据隔离

------

### 可扩展

支持后续扩展：

- AI模拟面试
- 面试题推荐
- 知识图谱

------

# 十二、项目亮点

本项目具备以下特点：

1. AI语音识别自动整理面试记录
2. 自动提取面试问题与答案
3. 构建结构化面试知识库
4. 支持复习与搜索
5. 完整的前后端分离架构





----

其他需求，本项目要上传到github所以格式要符合工程规范，地址为https://github.com/confucian9/InterviewPai.git

llm用deepseek，key在系统变量 DEEPSEEK_KEY

相关配置参考：

```yml
datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:paiflow-workflow}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
  username: ${MYSQL_USER:root}
  password: ${MYSQL_PASSWORD:1234}
  hikari:
    maximum-pool-size: 20
    minimum-idle: 5
    connection-timeout: 30000
    idle-timeout: 600000
    max-lifetime: 580000
    connection-test-query: SELECT 1
    validation-timeout: 5000
    
     # Redis 配置
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:123456}
      database: 0 # 注意需要和hub应用的保持一致
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 3000ms
          
         
        s3:
  endpoint: ${OSS_ENDPOINT:http://localhost:9000}
  remoteEndpoint: ${OSS_REMOTE_ENDPOINT:http://localhost:9000}
  accessKey: admin
  secretKey: 12345678
  bucket: ${OSS_BUCKET_CONSOLE:aitools}
  presignExpirySeconds: ${OSS_PRESIGN_EXPIRY_SECONDS_CONSOLE:600}
  enablePublicRead: true
 
         
```

语音识别使用阿里百炼平台的模型fun-asr

密钥在系统变量

OSS_ACCESS_KEY_ID

OSS_ACCESS_KEY_SECRET

使用示例

```java
import com.alibaba.dashscope.audio.asr.transcription.*;
import com.alibaba.dashscope.utils.Constants;
import com.google.gson.*;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.net.HttpURLConnection;
import java.net.URL;
import java.util.Arrays;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        // 以下为北京地域url，若使用新加坡地域的模型，需将url替换为：https://dashscope-intl.aliyuncs.com/api/v1
        Constants.baseHttpApiUrl = "https://dashscope.aliyuncs.com/api/v1";
        // 创建转写请求参数。
        TranscriptionParam param =
                TranscriptionParam.builder()
                        // 新加坡和北京地域的API Key不同。获取API Key：https://help.aliyun.com/zh/model-studio/get-api-key
                        // 若没有配置环境变量，请用百炼API Key将下行替换为：.apiKey("sk-xxx")
                        .apiKey(System.getenv("DASHSCOPE_API_KEY"))
                        .model("fun-asr")
                        // language_hints为可选参数，用于指定待识别音频的语言代码。取值范围请参见API参考文档。
                        .parameter("language_hints", new String[]{"zh", "en"})
                        .fileUrls(
                                Arrays.asList(
                                        "https://dashscope.oss-cn-beijing.aliyuncs.com/samples/audio/paraformer/hello_world_female2.wav",
                                        "https://dashscope.oss-cn-beijing.aliyuncs.com/samples/audio/paraformer/hello_world_male2.wav"))
                        .build();
        try {
            Transcription transcription = new Transcription();
            // 提交转写请求
            TranscriptionResult result = transcription.asyncCall(param);
            System.out.println("RequestId: " + result.getRequestId());
            // 阻塞等待任务完成并获取结果
            result = transcription.wait(
                    TranscriptionQueryParam.FromTranscriptionParam(param, result.getTaskId()));
            // 获取转写结果
            List<TranscriptionTaskResult> taskResultList = result.getResults();
            if (taskResultList != null && taskResultList.size() > 0) {
                for (TranscriptionTaskResult taskResult : taskResultList) {
                    String transcriptionUrl = taskResult.getTranscriptionUrl();
                    HttpURLConnection connection =
                            (HttpURLConnection) new URL(transcriptionUrl).openConnection();
                    connection.setRequestMethod("GET");
                    connection.connect();
                    BufferedReader reader =
                            new BufferedReader(new InputStreamReader(connection.getInputStream()));
                    Gson gson = new GsonBuilder().setPrettyPrinting().create();
                    JsonElement jsonResult = gson.fromJson(reader, JsonObject.class);
                    System.out.println(gson.toJson(jsonResult));
                }
            }
        } catch (Exception e) {
            System.out.println("error: " + e);
        }
        System.exit(0);
    }
}
```